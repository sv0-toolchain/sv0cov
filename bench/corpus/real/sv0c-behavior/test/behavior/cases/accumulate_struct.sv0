/* loop accumulating into a struct field via local — exits 60. */
struct Acc { total: i32 }
fn main() -> i32 { let mut t: i32 = 0; let mut i: i32 = 1; while i <= 5 { t = t + i * 4; i = i + 1; } let a: Acc = Acc { total: t }; return a.total; }
